exports.default = {
  hosts: function(api) {
    return {

      // ANYLINK.GET.ENDPOINT
      anylink_get_endpoint: "http://localhost:8080/dummy",

      // ANYLINK.POST.ENDPOINT
      anylink_post_endpoint: "http://localhost:8080/dummy", 

      // ANYLINK.NET.ENDPOINT
      anylink_net_endpoint: "localhost:25000"

    }
  }
}
